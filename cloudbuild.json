{
  "steps": [
    {
      "name": "gcr.io/cloud-builders/docker",
      "args": [
        "build",
        "-t",
        "europe-west2-docker.pkg.dev/${PROJECT_ID}/${REPO_NAME}/nest-auth:latest",
        "."
      ]
    },
    {
      "name": "gcr.io/cloud-builders/docker",
      "args": [
        "push",
        "europe-west2-docker.pkg.dev/${PROJECT_ID}/${REPO_NAME}/nest-auth:latest"
      ]
    }
  ],
  "options": {
    "logging": "CLOUD_LOGGING_ONLY"
  }
}
